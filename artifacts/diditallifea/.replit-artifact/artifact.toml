kind = "mobile"
previewPath = "/"
title = "MyLifelens"
description = "A mobile progress journal for documenting people and projects over time. Users can capture or upload progress photos, align new images against the previous one with a ghost overlay, schedule recurring reminders from every 6 hours through monthly, and share a finished timeline."
version = "1.0.0"
id = "artifacts/diditallifea"
router = "expo-domain"

[[services]]
name = "expo"
ensurePreviewReachable = "/status"
paths = [ "/" ]
localPort = 18112

[services.development]
run = "pnpm --filter @workspace/diditallifea run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/diditallifea", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/diditallifea", "run", "serve" ]

[services.env]
PORT = "18112"
BASE_PATH = "/"
